/**
 * @file
 * Navigation Styling
 *
 * Default menu styling (ul.menu) is defined in system-menus.css.
 */


/*
 * The active item in a Drupal menu
 */
li a.active {
  color: #000;
}

/*
 * Navigation bar
 */
#navigation{
 position:relative;
 top:-15px;
}
#navigation .section {
width:94%;
margin:auto;
background-color:green;
position:relative;
bottom:10px;
height:200%;
z-index:500;
-moz-box-shadow: 0px 0px 4px rgba(0,0,0,0.55);
  	-webkit-box-shadow: 0px 0px 4px rgba(0,0,0,0.55);
  	box-shadow: 0px 0px 4px rgba(0,0,0,0.55);
  	
   -webkit-border-radius: 2px;
    -moz-border-radius: 2px;
	border-radius: 2px;
}
 
#navigation ul {
margin:auto;
padding:0px;
padding-top:15px;
text-align:center;

}
#navigation ul li {
float:none;
display:inline;

font-size:20px;
color:#A4A4A4;

}
.region-navigation {
}

/*
 * Primary and Secondary menu links
 */
#main-menu {
}

#secondary-menu {
}

/*
 * Menu blocks
 */
.block-menu {
}

/*
 * "Menu block" blocks. See http://drupal.org/project/menu_block
 */
.block-menu_block {
}
#main-menu{
 position: relative;
z-index: 500;
}
.r-triangle-top {
   border-color: #088A08 transparent transparent  ;
   border-style:solid;
   border-width:25px;
   height:0px;
   width:0px;
   position: relative;
           float:right;
           bottom: 64px;
           left:0px;

}

.r-triangle-bottom {
   border-color: transparent transparent #088A08;
   border-style:solid;
   border-width:25px;
   height:0px;
   width:0px;
   position: relative;
        float: right;
        bottom: 80px;
        left:50px;
}

.l-triangle-top {
   border-color: #088A08 transparent transparent;
   border-style:solid;
   border-width:25px;
   height:0px;
   width:0px;
   position: relative;
   float: left;
   top: 1px;
   left: 2px;
}

.l-triangle-bottom {
   border-color: transparent transparent #088A08;
   border-style:solid;
   border-width:25px;
   height:0px;
   width:0px;
   position: relative;
   float: left;
   top: -19px;
   left: -45px;
}

